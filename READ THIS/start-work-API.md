GET /movies
    Hente ut lister med filmer brukeren har lagt til.

POST /movies
    Legge til ny filmer til brukers liste

PATCH /movies/:id
    Kunne oppdatere eksiterende filmer i listen

DELETE /movies/:id
    Kunne slette valgt(e) film(er) fra listen

